import { DNS } from "src/interfaces/validator-config.interfaces";

/**
 * DNS values. 
 * 
 * Do not change if You want to connect to correct network.
 * Change acceptable if something change in network.
 */
export const dns:DNS = {
        host: 'http://localhost:',
        port: 3000,
        type: 'router'
}

